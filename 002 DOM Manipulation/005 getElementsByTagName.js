function magic() {
  let b1 = Array.from(document.getElementsByName("div"));
  // console.log(b1)
  for (Element of div) {
    Element.classlist.add("black");
    Element.classlist.remove("black");
  }
}
